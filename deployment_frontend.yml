apiVersion: v1
kind: Pod
metadata:
  name: my-pod-test-group
spec:
  containers:
    - name: nginx
      image: unlegendnl/canvasairlinefrontend:tagname
      ports:
        - name: http
          containerPort: 80
          protocol: TCP
